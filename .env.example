# LLM credentials for Zoomify.
# Copy to `.env` and fill in your key. Pick the model in the app UI dropdown.

# Preferred: OpenRouter
OPENROUTER_API_KEY=
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Fallback: direct OpenAI (used when OPENROUTER_API_KEY is unset)
# OPENAI_API_KEY=
