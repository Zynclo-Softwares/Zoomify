# OpenAI credentials & model selection for Zoomify.
# Copy to `.env` and fill in your key.

OPENAI_API_KEY=
# Any vision + tool-calling capable model, e.g. gpt-4o, gpt-4o-mini, gpt-4.1
OPENAI_MODEL=gpt-4o
